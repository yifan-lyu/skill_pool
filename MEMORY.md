# Project Memory

Durable lessons that should bear on future sessions. Appended by `/done` when something non-obvious is learned.

Format: `[LEARN:category] short, durable lesson — why / how to apply`

Categories used: `workflow`, `data`, `methods`, `code`, `writing`, `tooling`, `citation`.

---

<!-- Append new entries below. Most recent at bottom. Keep entries short — one or two lines. -->
